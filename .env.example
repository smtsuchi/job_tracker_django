# Django Settings
# Generate a secure secret key for production: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=your-secret-key-here

# Debug mode - set to False in production
DEBUG=True

# Allowed hosts - comma-separated list of domains
# Example for local: 127.0.0.1,localhost
# Example for production: your-app.onrender.com,yourdomain.com
ALLOWED_HOSTS=127.0.0.1,localhost

# CORS Settings (optional - if you need to add additional origins)
# The app already has localhost:3000 and cs-job-tracker.web.app configured
# Add any additional frontend domains here if needed
# CORS_ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend.com

# Database (optional - only needed if switching to PostgreSQL)
# For SQLite (default), no DATABASE_URL needed
# For PostgreSQL on Render, use the DATABASE_URL they provide:
# DATABASE_URL=postgresql://user:password@host:port/database
